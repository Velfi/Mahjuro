# Python deps for the scripts/ helpers.
#
# Install with (use one Python version — mixed 3.13/3.14 venvs break imports):
#     python3.13 -m venv .venv
#     source .venv/bin/activate
#     python -m pip install -r scripts/requirements.txt
#     export GEMINI_API_KEY="..."   # or GOOGLE_API_KEY
#
# Grouped by which scripts need what — you can slim this down if you only
# run a subset.

# Google Gemini SDK — every generate_*.py / gen_*.py uses Nano Banana 2
# (`gemini-3.1-flash-image-preview`) via scripts/_image_gen.py
# (temptations, boss icons, relic art, …).
google-genai

# Pillow — image load/save/resize in most generator scripts.
Pillow

# Background removal on *_object.png renders (generate_relic_art --bg-removal).
# onnxruntime is the ML runtime rembg loads u2net through.
rembg
onnxruntime
