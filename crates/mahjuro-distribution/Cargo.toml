[package]
name = "mahjuro-distribution"
version.workspace = true
edition.workspace = true
publish.workspace = true

[features]
default = []
steam = ["dep:steamworks", "dep:mahjuro-assets", "dep:image"]
macos-store = []
windows-store = []

[dependencies]
mahjuro-core.workspace = true
mahjuro-assets = { workspace = true, optional = true }
dirs.workspace = true
enum_dispatch.workspace = true
log.workspace = true
steamworks = { workspace = true, optional = true }
image = { workspace = true, optional = true, features = ["png"] }

[target.'cfg(target_os = "macos")'.dependencies]
block2 = "0.6"
objc2 = "0.6"
objc2-foundation = { version = "0.3", features = [
    "NSArray",
    "NSError",
    "NSString",
    "NSURL",
] }
objc2-app-kit = { version = "0.3", default-features = false, features = [
    "NSApplication",
    "NSPanel",
    "NSResponder",
    "NSSavePanel",
    "NSViewController",
    "NSWindow",
    "NSWorkspace",
] }
objc2-game-kit = { version = "0.3", default-features = false, features = [
    "GKBasePlayer",
    "GKAchievement",
    "GKLocalPlayer",
    "GKPlayer",
    "block2",
    "objc2-app-kit",
] }

[target.'cfg(target_os = "windows")'.dependencies]
windows = { version = "0.61", features = [
    "Win32_Foundation",
    "Win32_UI_Shell",
    "Win32_UI_Shell_Common",
    "Win32_System_Com",
    "Win32_Graphics_Gdi",
] }

[build-dependencies]
cc = "1.2"
