#!/bin/sh
# AppImage entry point. Locate the bundled binary regardless of how the
# AppImage was invoked, then exec it with the original arguments.
HERE="$(dirname "$(readlink -f "$0")")"
exec "$HERE/usr/bin/mahjuro" "$@"
